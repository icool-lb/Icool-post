export const runtime='nodejs'; export const dynamic='force-dynamic'; export const maxDuration=20;
const FALLBACKS={hvac:['Precision Installation, Built to Last.','Comfort in Every Space, Quality in Every Detail.'],solar:['Clean Energy, Built for Tomorrow.','Harvesting the Sun, Powering Everyday Comfort.'],mep:['Reliable Systems, Professional Execution.','Engineering Details That Last.'],construction:['Building Excellence, Detail by Detail.','Quality Construction, Built with Confidence.'],lighting:['Beautiful Pathways, Professionally Illuminated.'],inventory:['Quality Units, Ready to Deliver.'],team:['People Behind Smart Energy.'],auto:['Quality Work, Delivered with Confidence.','Professional Solutions for Real Projects.']};
function fb(k='auto'){const a=FALLBACKS[k]||FALLBACKS.auto; return a[Math.floor(Math.random()*a.length)]}
export async function GET(){return Response.json({ok:true,version:'v12-pro',message:'iCOOL slogan API is working'})}
export async function POST(req){try{const b=await req.json().catch(()=>({})); const {image,industry='auto'}=b; if(!process.env.OPENAI_API_KEY||!image) return Response.json({slogan:fb(industry),source:'fallback'}); const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-4.1-mini',input:[{role:'user',content:[{type:'input_text',text:`Create one short premium marketing slogan for iCOOL based on this image. Industry hint: ${industry}. English only. Max 9 words. No brand names, hashtags, emojis, or quotation marks.`},{type:'input_image',image_url:image}]}]})}); if(!r.ok) return Response.json({slogan:fb(industry),source:'fallback'}); const d=await r.json(); const t=String(d.output_text||'').replace(/["“”]/g,'').trim(); return Response.json({slogan:t||fb(industry),source:t?'openai':'fallback'})}catch{return Response.json({slogan:fb('auto'),source:'fallback'})}}
